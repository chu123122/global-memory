# control-panel-v1 · SPEC 桥接文档

> 本文件用于兼容 `harness/verify_workflow.py` 的旧版 `docs/` 产物约定。

权威 SPEC 在上一层：

- `../SPEC.md`

当前验收边界：

- 左侧导航区支持鼠标滚轮。
- V1 主控台只读状态、同步预览、维护报告、外部事件、AI 诊断/计划入口可用。
- GUI 高风险动作仍需二次确认。
- AI Runner V1 不允许自动执行改文件。

