# Requirements · harness-governance-v1

> 文档类型：需求分析
> 创建：2026-04-24
> 任务目录：`projects/harness-governance-v1/`
> Status: discussion

## 1. 这是什么

把当前的个人 harness 从"会搭"推到"会运营"。手段是 5 步收敛动作：memory 垃圾回收、状态面板升级、规则到检查的映射表、hook 回归测试、任务结果账本。**不再扩建组件**——所有产物都是把现有 hooks/scripts/skills 的运行状态变得可见、可验证、可度量。

`control-panel-v1` 收编为本任务的 Phase 1-B 子项,GUI 验收+commit 照做,但视角从"做完一个工具"变成"harness 状态层第一块"。

## 2. 现状盘点

| 维度 | 当前状态 | 评级 | 证据 |
|---|---|---|---|
| Context map | OPERATIONS / ARCHITECTURE / MEMORY 三件套已成型 | 较强 | `~/.claude/global-memory/_bootstrap/docs/` |
| Workflow | `/work skill` 主会话流程已上线 | 较强 | `decision_work_mode_workflow.md` |
| Runtime guardrails | 6 个 hooks 已挂载 | 中上 | dangerous_command / memory_protector / doc_gate / audit / subagent / post_task |
| Deterministic checks | verify 系列脚本完整 | 中上 | `verify_memory.py` / `check_health.py` / `bootstrap.py check` |
| Observability | audit_logger 已写盘,无聚合视图 | 中 | `tool_audit.jsonl` / `subagent_audit.jsonl` |
| Evaluation loop | 无任务结果记录 | **弱** | — |
| Memory governance | **超限污染中** | **偏弱** | MEMORY.md 显示 59/50 |
| Subagent 语义 | OPERATIONS 仍把 learning/work 描成 subagent,与 work mode 决策矛盾 | **需收敛** | `decision_work_mode_workflow.md` vs `OPERATIONS.md` |
| Recovery | post_task_hook 已存在,无 smoke 验证 | 中 | — |

总判断:**骨架完整,缺"状态可见 + 结果可评估 + 记忆可清理"。**

## 3. 6 个缺口

### 缺口 1 — 主控面板还只是文档,不是状态面板

OPERATIONS.md 回答"系统怎么运作",但回答不了"现在 hooks 是否真在跑、memory 是否超限、两个 repo 是否 dirty、上次 post_task_hook 是否成功"。`control-panel-v1` 的 GUI 已经做了一部分(Git 状态卡片、事件页、结论面板),但缺机器可读的 `harness_status.py` CLI 入口,且没有覆盖 hook 触发证据。

### 缺口 2 — 运行证据没闭环

6 个 hooks 都写在 `settings.json` 里,但没有"hook 真的拦截过一次危险命令"的回归证据。文档断言 ≠ 运行证据。

### 缺口 3 — Memory 已超限,必须 GC

MEMORY.md 显示 59/50。已经是上下文污染源。继续加 memory 文件等于把"个人 AGENTS.md"问题搬到这里重演一次。

### 缺口 4 — 规则到检查的映射不显式

rules / memory-rules / conventions / hooks / scripts / skills 之间没有 trace 表。哪条规则是 hard(deny)、soft(warn)、prompt-only(纯说明),没人答得上。

### 缺口 5 — 评估回路缺失

没有任务结果账本,无法判断"加了 doc_gate 后返工率是否下降"。规则有没有让结果更稳定,只能凭感觉。

### 缺口 6 — 文档与实现漂移

`decision_work_mode_workflow.md` 已决定 work 是主会话模式,但 `OPERATIONS.md` 仍在 "Subagents" 语义下描述 learning/work。这种漂移只能靠扫描脚本检出,不能靠人脑记。

## 4. 范围

### 4.1 本期纳入(Phase 0 → Phase 4)

> 2026-04-24 重排:基于"反馈价值"维度升级 Phase 0(文件名约定,原 §10.2 候选)和 Phase 4-A(账本骨架,从 P2 拆出)。Phase 编号保留主题归属,执行顺序单列。详见 `feedback/feedback_collaboration_meta.md` §1。

| 执行序 | Phase | 缺口 | 产物 | 优先级 |
|---|---|---|---|---|
| **1** | **Phase 0** | 缺口 7(原 §10.2) | `/work` 流程文件名约定修复:人类向文档(需求分析/设计文档)用中文名,AI 向(SPEC/HANDOFF)保留大写英文。涉及 work skill 模板、registry `human_doc_patterns`、verify 脚本 grep、存量任务文档批量迁移策略 | **P0** |
| **2** | **Phase 4-A** | 缺口 5 骨架 | `~/.claude/logs/task_outcomes.jsonl` 写入端骨架:`panel_api.py outcome` 子命令 + schema v1(5-7 字段) + 写入约定。**纯写入,不做 reader/可视化**。本任务自身从此刻起就是第 1 条样本 | **P0**(从 P2 升,反馈价值★★★ 时间不可逆) |
| 3 | Phase 1-A | 缺口 3 | `harness/memory_gc.py` + 当场跑一次,把 60 降到 ≤ 45 | P0 |
| 4 | Phase 1-B | 缺口 4 v1 | `_bootstrap/docs/RULE_ENFORCEMENT_MATRIX.md` 纯文档静态梳理(rule × layer × enforcer × failure × smoke_test_id),覆盖现有 6 hooks + 已知 hard rules。无脚本、无自动校验 | P0 |
| 5 | Phase 1-C | 缺口 1 | `control-panel-v1` 收尾:GUI 实机验收、commit/push、补 V9/V10 验收(对应 REVIEW-2026-04-24-1445.md 的 6 条建议)。可引用 1-B 的矩阵验证 hooks enforcement 是否说得清 | P0 |
| 6 | Phase 2-A | 缺口 1 续 | `harness/harness_status.py` CLI(汇总 hooks 安装状态、最近触发时间、memory 计数、repo 状态、active tasks) + `STATUS_SNAPSHOT.md` 落盘 | P0 |
| 7 | Phase 2-B | 缺口 4 v2 + 缺口 6 | `harness/verify_doc_drift.py` 扫描脚本:对照 1-B 矩阵自动校验文档断言与实现/决策的偏离;首版 5 条核心规则 | P1 |
| 8 | Phase 3 | 缺口 2 | `harness/smoke_test_hooks.py` 回归集(覆盖 6 个 hooks 各 1 个 happy + 1 个 fail 路径);smoke_test_id 反向写回 1-B 矩阵 | P1 |
| 9 | **Phase 4-B** | 缺口 5 完整 | `panel_api.py` 双通道 reader + 面板"账本"页 + 趋势分析。基于 4-A 已积累的样本 | P2 |
| 10 | **Phase 5** | V9 (REVIEW 修订:V9 在原 9 Phase 中无承接) | OPERATIONS.md / ARCHITECTURE.md 收敛 — 把 learning / work 从 "subagents" 语义改为 "modes",subagent 词仅留给 design-reviewer / guardian / explore;grep 验证 + 人工 review | P1 |

**Phase 0 / Phase 4-A 升级理由**(对应 feedback §1 的"反馈价值"维度):
- **Phase 0**:文件名约定改名工作量随时间膨胀,每多 1 个新任务多 2 个文件要 mv;且影响后续所有 Phase 的 DESIGN 文件命名,提前定型避免反复
- **Phase 4-A**:本任务自身就是样本——账本晚 1 天上线就永远丢 1 天数据,**时间不可逆**。骨架可极轻量(写入端 + JSONL append),不阻塞任何 Phase

**Phase 1-B 仍前置于 1-C/2-A/2-B/3 的理由**:纯文档,无代码依赖,半小时即可产出 v1;后续所有 Phase 都需要引用这张表来描述自己拦截的规则与对应的 smoke test,提前定型可避免反复改写。

### 4.2 暂不纳入

- 不接 Droid / 不加 MCP / 不引入 PySide / 不开 HTTP API(都不是当前瓶颈)
- 不再新增 subagent(现有 design-reviewer / guardian / explore 已够用)
- 不写新的 skill 入口(收敛优先,扩建延后)
- **不再新增 memory 文件**——本期所有产物都是脚本 / JSONL / 已有 docs 的修订,不进 memory

## 5. 验收标准

| # | 验收项 | 标准 | 验证方法 |
|---|---|---|---|
| V1 | memory 总数 ≤ 45 | `verify_memory.py` 报 0 ERROR + 0 WARNING(文件数) | CLI 跑 + 看 `MEMORY.md` 头部计数 |
| V2 | memory_gc 可重复执行 | 干跑两次第 2 次无新归档 | `memory_gc.py --dry-run` |
| V3 | control-panel-v1 V9/V10 落地 | REQUIREMENTS §4 增加结论面板优先 + 模型层单测两条;SPEC §3 每条加 `[V*]` 编号 | 文件 diff 比对 |
| V4 | `harness_status` 一行命令出全景 | 输出包含:7 hooks installed/last_triggered、memory 计数、2 repo 状态、active tasks 数、last post_task_hook、next_action | `python harness/harness_status.py --json` |
| V5 | RULE_ENFORCEMENT_MATRIX 完整 | 每个 hook + 每条 hard rule 至少 1 行,字段:rule / layer(hard|soft|info) / enforcer / failure_behavior / smoke_test_id | 人工 review + 表格行数 ≥ 14 |
| V6 | 文档漂移扫描可用 | 至少检出 1 条已知漂移(OPERATIONS.md 的 subagent 描述 vs work mode 决策),并报告 | `verify_doc_drift.py --json` |
| V7 | 7 hooks 都有 smoke | smoke_test_hooks 覆盖 7 个 hook 文件 × happy/fail = 14 个用例,全 PASS | `python harness/smoke_test_hooks.py --json` |
| V8 | 结果账本可读可写 | `panel_api.py outcome` 子命令追加一条 JSONL;面板"账本"页能展示最近 10 条 | CLI + GUI 双验 |
| V9 | OPERATIONS / ARCHITECTURE 与决策一致 | learning / work 改称 "modes" 而非 "subagents";subagent 仅指 design-reviewer / guardian / explore | grep + 人工 review |

## 6. 风险与边界

### 风险表(现方案 → 代价 → 新方案)

| 风险 | 现方案的代价 | 新方案 |
|---|---|---|
| memory_gc 误删用户内容 | 直接 `rm` 风险高 | gc 永远先 `mv` 到 `archive/<date>/`,7 天后才允许真删;首次跑 `--dry-run` |
| harness_status 把 jsonl 全量读盘 | 文件膨胀后 GUI 卡顿 | 维护 `last_offset`,只读增量(同步 control-panel-v1 REVIEW 风险 2 的解法) |
| 文档漂移扫描误报刷屏 | 噪音覆盖真信号 | 漂移规则白名单 + 严重度分级(error / warning / info);首版只跑 5 条核心规则 |
| smoke_test_hooks 触发真危险命令 | 真把仓库搞坏 | 所有 fail 路径走 sandbox 目录(`/tmp/harness_smoke/`),不碰真实文件 |
| 任务结果账本 schema 早期反复改 | 历史数据无法回放 | 先 `schema_version: 1` 字段,后续 reader 按 version 兼容;首版字段最少化 |

### 边界

- 本任务不替代现有 work skill / agents / hooks——只是给它们装"仪表盘 + 体检 + 账本"
- 不重写 control-panel-v1 已交付的代码,只在它之上加 CLI 入口和扩展页签
- 不强制改 settings.json hooks 配置,除非 smoke 暴露真问题

## 7. 与 control-panel-v1 的关系

| 维度 | control-panel-v1 | harness-governance-v1 |
|---|---|---|
| 视角 | 做一个 GUI 工具 | 把 harness 变成可运营系统 |
| 范围 | maintain.py + GUI + 事件 API + AI adapter | 上述 + memory GC + 状态 CLI + 规则映射 + smoke + 账本 |
| 文档关系 | 物理位置不动,仍在 `projects/control-panel-v1/` | 在 REQUIREMENTS §4 引用为 Phase 1-B,不迁移文件 |
| 收尾时机 | Phase 1-B 完成时一并 commit | 各 Phase 独立 commit |

## 8. 后续方向(本期不做,标记触发条件)

| 方向 | 触发条件 |
|---|---|
| PySide / Web UI | Tkinter 渲染 STATUS_SNAPSHOT 出现明显瓶颈 |
| 本地 HTTP API | 出现 ≥ 1 个无法走 JSONL 文件总线的真实外部消费者 |
| MCP 工具集成 | 当前 Bash/Read/Edit 出现明显能力缺口 |
| 任务结果可视化(图表) | 账本积累 ≥ 50 条,有趋势分析价值 |
| AI 半自治 fix(白名单 execute) | 账本显示某类返工 ≥ 5 次且都是同样的修复模式 |

## 9. 不做的事

- 不再新增 subagent
- 不再新增 memory 文件
- 不接 Droid / MCP / PySide / HTTP
- 不重写已交付代码
- 不为了凑文档完整性补"暂无"章节

## 10. 待并入条目(增量记录,后续分配到 Phase)

> 用户在讨论阶段追加的问题,本节先按发现顺序记录,确定方案后再合入 §3 缺口表 + §4 Phase 表。

### 10.0 候选条目收敛规则(REVIEW-2026-04-24-1601 修订)

**触发**:每条候选条目添加时打 `添加日期`;每 3 天 review 一次本节;条目超过 7 天未升 Phase → 强制三选一:
- **升 Phase**:在 §4.1 加新行,本节标"已合入 Phase X"
- **暂不纳入**:移到 §4.2 暂不纳入列表,本节删除条目
- **延长 1 周**:必须给"为什么不能现在决"的明确理由

**目的**:防止 §10 变成只增不减的待办堆(REVIEW 风险)。

**规则触发示例**:§10.1 添加日期 2026-04-24,deadline 2026-05-01。

### 10.1 auto-fix commit 噪音污染(2026-04-24 记录,deadline 2026-05-01)

**现象**:`git log --oneline` 最近 10 条里有 8 条是 `auto-fix: <timestamp> [post_task_hook]`,关键人工修改被淹没。

**证据**(commit `b99235a` 之前):
```
2362ec9 auto-fix: 20260424_120843 [post_task_hook]
48620f3 auto-fix: 20260424_120336 [post_task_hook]
4cc16a5 docs: add human maintenance guide          ← 唯一可读的人工 commit
eea9a88 auto-fix: 20260424_114748 [post_task_hook]
59ded2b auto-fix: 20260424_114633 [post_task_hook]
12ef544 auto-fix: 20260424_114124 [post_task_hook]
914eaa6 auto-fix: 20260424_113859 [post_task_hook]
2a67930 auto-fix: 20260424_113406 [post_task_hook]
```

**影响**:
- 无法 review 历史,找不到关键修改
- `git log` 失去价值,等于没有 commit 历史
- 一次会话产生 5-10 个同质 commit,仓库膨胀

**根因**:`post_task_hook` 每次 Stop 都 commit 当前所有 dirty,且 message 模板单一(只有时间戳)。

**候选方向**(待 DESIGN 阶段评估):
- A. 改为 stash/标记不 commit,只在用户显式触发时打包
- B. auto-commit 走单独分支(`auto-fixes/*`),主分支保持人工 commit
- C. message 模板带文件清单摘要 + 变更类型分类
- D. 同一会话内的多个 auto-fix 在 Stop 时 squash 成一个

**归属候选**(REVIEW 修订:Phase 1-D 在 §1.8 / §4 不存在,改命名):**Phase 6**(在 Phase 5 后插入)或 **Phase 3 之内**(smoke 周边),deadline 2026-05-01 前必须升 Phase 或丢弃。

### 10.4 dangerous_command_blocker false-positive on commit messages(2026-04-24 记录,deadline 2026-05-01)

**触发**:Phase 3 commit 时,commit message 引用了 "broad recursive delete" 字串作为测试用例描述,被 dangerous_command_blocker hook 拦截。

**根因**:hook 正则 `\brm\s+.*-[a-zA-Z]*r[a-zA-Z]*\s+(/\s|/\s*$|~|"\s*~|\*\s*$)` 对 commit message 字串里的引用也命中——hook 看不到这是 git commit 还是真 bash 命令,只看 stdin.tool_input.command 字串。

**症状**:正常的 commit 流程被中断,需要手动 sanitize message。

**候选方向**:
- A. hook 区分 `Bash` vs `git commit` 模式(看 command 是否以 `git commit` 开头则放宽)
- B. hook 加白名单关键词(如 `commit -m` 出现时放行所有内容)
- C. 文档约定 commit message 不引用 dangerous patterns(转义如 `[r]m -rf` 等)

**归属候选**:Phase 3 v2(扩 smoke + 修 hook 一并做)。

### 10.3 control-panel-v1 GUI 重做(2026-04-24 记录,**已立独立任务 control-panel-v2-pyside**)

**触发**:Phase 1-C 实机验收时用户反馈 v1 Tkinter 滚动套娃 + 观感差,现场决定换技术栈到 PySide6。

**5 选项对比 + 选定 PySide6** 已写入 `projects/control-panel-v2-pyside/需求分析.md` §3。

**影响本任务**:
- **Phase 1-C** 语义微调:不再"GUI 实机验收 + commit/push",改为"v1 留作历史版本,v2 接管未来 GUI 工作"
- **Phase 4-B** 账本可视化:基于 PySide6 实现,不再依赖旧 Tkinter
- **Phase 5** OPERATIONS / ARCHITECTURE 收敛:范围扩大,同步更新 control-panel-v1 §3.2 边界声明("不引入 PySide" → "V1 不引入,V2 已立项")

**新任务**:`projects/control-panel-v2-pyside/`(已建,需求/设计文档 Status: discussion)。**实现由独立对话推进**(用户决策),与本任务并行。

**归属候选**:不进本任务 Phase 表(独立任务),只做 Phase 1-C / 4-B / 5 的语义微调。

### 10.2 /work 流程文件名约定 bug(2026-04-24 记录,**已合入 Phase 0**)

> **状态**:已正式纳入 §4.1 Phase 0(执行序 1,P0)。本节保留作为问题描述源头,不再独立追踪。决策点(命名格式、范围、过渡策略)在 Phase 0 设计阶段展开。


**现象**:`/work` 流程产出的人类向文档(REQUIREMENTS / DESIGN)使用了大写英文文件名,与 style-refs 样例的中文命名约定不一致。

**期望约定**:

| 受众 | 文件类型 | 文件名约定 | 当前样例 |
|---|---|---|---|
| 给人看 | 需求分析 / 设计文档 | **中文**,如 `需求分析.md` / `设计文档.md`(对应 style-refs 的 `【需求分析】XX.md`) | ❌ 当前是 `REQUIREMENTS.md` / `DESIGN.md` |
| 给 AI 看 | 规格 / 交接 | **大写英文**,如 `SPEC.md` / `HANDOFF.md` | ✅ 已是 |

**影响范围**(已被错误命名的文件):
- `projects/control-panel-v1/REQUIREMENTS.md` `DESIGN.md`
- `projects/harness-governance-v1/REQUIREMENTS.md`(本任务自身,本 commit 也踩坑)
- 其他 active_tasks 下的 REQUIREMENTS / DESIGN 文件均同样

**根因**:`/work skill` 流程模板未区分"人类向"和"AI 向"产物的命名约定;`project_registry.json` 的 `human_doc_patterns` 字段也写死了 `["REQUIREMENTS.md", "DESIGN.md"]`,放大了错误约定。

**修复涉及**:
- `D:/global-memory/skills/work/v1/SKILL.md` 流程模板
- `~/.claude/projects/project_registry.json` 的 `human_doc_patterns` 字段
- `harness/verify_docs.py` 等所有引用大写文件名的脚本(grep 范围广)
- 历史文件的 `git mv` 批量改名(影响所有 active_tasks)

**归属候选**:Phase 0(在 1-A 之前,优先于一切——改了之后所有 Phase 的"设计文档"才能用对的命名),且需评估是否对存量任务文档全量改名,还是新任务起强制约束、老任务遇到时再改。
